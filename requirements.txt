# ================================================================
# Polymarket Weather Trading Bot — Requirements
# Python 3.11+   |   pip install -r requirements.txt
# ================================================================

# --- Polymarket Official SDK ---
py-clob-client>=0.17.0

# --- HTTP ---
requests>=2.31.0
urllib3>=2.2.0

# --- Data / Math ---
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# --- Scheduling ---
APScheduler>=3.10.4

# --- Config / Env ---
python-dotenv>=1.0.0

# --- Storage ---
# SQLite is stdlib; SQLAlchemy for clean ORM + future Postgres migration
SQLAlchemy>=2.0.30

# --- Notifications ---
python-telegram-bot>=21.3
requests>=2.31.0   # Discord webhook via plain POST

# --- Utilities ---
tenacity>=8.3.0    # retry with exponential back-off
cachetools>=5.3.3  # TTL in-memory cache
arrow>=1.3.0       # sane datetime handling
rich>=13.7.0       # coloured console output
click>=8.1.7       # CLI flags (--paper / --live / --backtest)

# --- Testing ---
pytest>=8.2.0
pytest-mock>=3.14.0
responses>=0.25.0  # mock HTTP calls in unit tests

# --- API Server ---
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0
